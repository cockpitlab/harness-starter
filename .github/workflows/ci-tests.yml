name: ci / tests

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Harness tests placeholder
        run: |
          echo "✅ [Harness Engineering] tests check passed (placeholder for now)"
          # TODO: replace with real tests later